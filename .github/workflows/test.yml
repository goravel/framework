# The badge is generated by the following workflow, the status will be fake when adding pull_request on the `on` field.
# so we add the test_pr.yml separately
name: Test
on:
  push:
    branches:
      - master
jobs:
  test:
    uses: goravel/.github/.github/workflows/test.yml@master
    secrets: inherit
